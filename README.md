
## Run Locally

**Prerequisites:**  Node.js


1. Install dependencies:
   type this in cmd npm install then when it done type npm run build
2. Run the app:
   go to dist folder and execute index.htlm with ur browser and done
